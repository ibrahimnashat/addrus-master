import 'package:flutter/material.dart';

class MColors {
  static Color appColor = Color(0xff526DFC);
  static Color blackApp = Colors.black;
  static Color hintColor = Colors.grey;
  static Color highLight = Colors.grey.withOpacity(0.05);
}
